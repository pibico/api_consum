"""AI skills layer — domain-specific LLM capabilities for api_consum.

Generic foundation reused across domains: contracts first, invoices later.
Import the registry to resolve a skill by name:  `from app.services.ai import registry`.
"""
